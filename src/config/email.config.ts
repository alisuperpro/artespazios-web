export const EMAIL_CONF = {
  host: import.meta.env.SECRET_EMAIL_HOST,
  port: import.meta.env.SECRET_EMAIL_PORT,
  email: import.meta.env.SECRET_EMAIL,
  pass: import.meta.env.SECRET_EMAIL_PASSWORD,
  name: import.meta.env.SECRET_EMAIL_NAME,
}
